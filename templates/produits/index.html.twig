{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} - Nos produits{% endblock %}

{% block body %}
<h1 class="text-center m-5  text-primary">Nos produits</h1>
<div class="container text-center px-4">
<section class="row gx-4 gy-3">
{# <div class="mt-3 row gx-2"> #}
{% for produit in produits %}

<div class="col-md-4">
    <div class="card bg-light mb-3">
        <div class="card-header"><img src="{{ produit.photo }}" alt="" class="img-fluid"></div>
        <div class="card-body">
            <h4 class="card-title text-center">{{ produit.titre }}</h4>
            <p class="card-text text-center">{{ produit.prix }}€</p>
            <div class="text-center">
           <a  class="btn btn-primary mb-3" href="{{ path('voir', {id: produit.id}) }}">VOIR</a>
           <br>
           <a  class="btn btn-info" href="{{ path('cart_add', {id: produit.id}) }}">Ajouter au panier</a>

            
            </div>
        </div>
    </div>
</div>
{# </div> #}

{% endfor %}
</section>

</div>



{% endblock %}
